<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Examen_CRUD" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RecipeIngredient" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personaliza el tema en tonos oscuros -->
        <item name="android:windowBackground">@android:color/background_dark</item>
        <item name="android:colorPrimary">@color/generalBackgroundPrimary</item>
        <!-- Status bar color -->
        <item name="colorPrimary">@color/generalStatusBarColor</item>
        <!-- Otros atributos de estilo personalizados -->
    </style>
</resources>